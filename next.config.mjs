const nextConfig = {
  output: "export",

  basePath: "/gpredict",
  assetPrefix: "/gpredict/",

  images: {
    unoptimized: true,
  },

  typescript: {
    ignoreBuildErrors: true,
  },
}

export default nextConfig
